export enum EOrderStatus {
  ORDERED = 'ordered',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
  PENDING = 'pending',
  PROCESSING = 'processing',
}

export enum EOrderType {
  DINEIN = 'Dine In',
  TAKEAWAY = 'Take Away',
}
